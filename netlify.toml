# Netlify 배포 설정 — 정적 사이트 빌드 후 dist/ 발행
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# 구 URL(/seoul/*) → 루트로 영구 이동
[[redirects]]
  from = "/seoul/*"
  to = "/:splat"
  status = 301
  force = true
